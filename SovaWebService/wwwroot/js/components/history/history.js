define(['knockout'], function (ko) {
    return function (params) {
        var title = ko.observable("Component History");
        return {
            title
        };

        //create component which return most recent visited posts

    }
});